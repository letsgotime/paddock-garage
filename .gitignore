node_modules/
.wrangler/
.dev.vars
.DS_Store
*.log

# agent tooling (installed per-machine via `npx skills add cloudflare/skills`)
.agents/
.claude/
skills-lock.json

# never commit private keys (the Tesla public key IS committed, deliberately)
*-private.pem
*.key
.garage/
data/inbox/

# generated from live gig-income data; this repo is PUBLIC, so it never gets committed
private-src/income.html
# the gated tier: every dollar, rendered by tools/runway.py from `gig.py export --full` and
# ~/.garage/runway.json; its own Worker behind Cloudflare Access; never committed
private-src/runway/

# compiled helpers, rebuilt by tools/gig.py on first run
tools/bin/
